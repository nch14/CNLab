<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cn.chenhaonee.cnlab">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".WelcomeActivity" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".StuMainActivity"
            android:label="@string/title_activity_stu_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".TeaMainActivity"
            android:label="@string/title_activity_tea_main" />
        <activity android:name=".tea.StudentsListActivity" />
        <activity android:name=".tea.StudentScoreListActivity"></activity>
    </application>

</manifest>